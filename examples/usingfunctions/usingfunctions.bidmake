import dlang;

define dirpath outDir "out";
define dirpath srcDir "src";

dlang.compile "usingfunctions"
{
    targetType exe;
    outputDirectory outDir;
    source path(srcDir, "usingfunctions.d");
}
